services:
  redis:
    image: redis:7.2.1
    expose:
      - "6379"
  db:
    image: postgres:15-alpine
    environment:
      - POSTGRES_USER=yaltt
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=yaltt
